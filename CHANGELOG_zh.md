<!-- Keep this file synchronized with CHANGELOG.md. -->

# 变更日志

[English](CHANGELOG.md)

Auto Script for GitHub Setup and Push 的重要变更记录在此文件中。

## 2.1.1 - 2026-08-31

### 变更

- 所有单选菜单统一使用连续数字。选择账号时按 `1`、`2`、`3` 等序号输入；`0` 始终用于返回或取消；针对某项明确操作的确认则继续直接询问“是”或“否”。
- 历史版本导入中的“添加另一个账号”不再使用字母选项，改为紧接现有账号序号的下一个数字。
- 中英文 README 已补充统一的输入规则，并新增回归检查，防止菜单以后重新出现字母选项。

## 2.1.0 - 2026-08-31

### 新增

- 新增明确的项目状态检查阶段：显示准确的仓库根目录和当前分支，保留已有 Git 历史，并在 detached HEAD、冲突未解决或 Git 操作尚未完成时安全停止。
- 新增依据 origin 识别账号的流程：遇到 `git@github800:owner/repository.git` 这类 SSH 地址时，先核对该主机名指定的密钥及其实际 GitHub 用户名，再考虑其他账号。
- 新增暂存前取消、空仓库结束和 update 只读取消保证，并为这些边界补充回归测试。
- 仓库地址新增支持 `git://github.com/owner/repository.git`。

### 变更

- 默认流程重新按照项目状态、origin、账号、具体密钥、目标仓库权限、改动检查、提交和上传的顺序组织。
- 已有仓库会明确说明不会再次执行 `git init`，并保留提交记录、分支、暂存内容和远端设置。
- 中央仓库中由 Git 跟踪的 `g.sh` 现在用于操作中央仓库本身；直接运行 `git-auto.sh` 则始终进入中央管理。
- 多个 SSH 主机名引用同一把私钥时只验证一次，减少重复等待和输出。
- 中英文界面文案全面改为说明每一步涉及的具体密钥、主机名、仓库、账号、文件、命令和写入边界。
- 中英文 README 已同步更新，与实际操作顺序和本机改动范围保持一致。

### 修复

- 修复已有仓库已经使用有效的 GitHub SSH 主机名时，仍进入通用账号创建流程或询问是否另建密钥的问题。
- 修复中文文案中的变量紧接全角标点时，被 Bash 误解析为其他变量名的问题。
- 修复只读识别或取消 update 时仍重写本地中央脚本路径设置的问题。
- 修复中央仓库中由 Git 跟踪的 `g.sh` 被当成其他项目本地启动器处理的问题。

## 2.0.0 - 2026-08-31

### 新增

- 新增唯一的完整中央引擎 `git-auto.sh`，供所有具体项目共同使用。
- 新增由 Git 跟踪的通用根目录 `g.sh`，可直接从公开仓库复制，且不包含个人路径或账号资料。
- 支持自动创建和修复小型项目本地 `g.sh`；启动器继续提供四个日常入口，并把自身文件夹准确传给中央引擎作为项目根目录。
- 新增完整双语的中央管理菜单，统一处理启动器、共享账号、检查修复、偏好设置和高级工具。
- 新增被 Git 忽略的 `private/config.txt`，集中保存界面语言、显示模式和所有共享 GitHub 账号基本资料。
- 自动把私人目录和配置文件权限限制为只有当前用户可以访问。
- 中央文件夹移动后，项目启动器会明确说明原路径失效及修复方法。

### 变更

- 以一个公开中央引擎和一份私人文本配置，取代重复的公开版／个人版完整脚本。
- 所有个性化内容均移出可执行代码；更新账号或偏好时不再修改 `git-auto.sh` 或项目启动器。
- 中央仓库根目录 `g.sh` 保持公开并由 Git 跟踪；中央菜单为其他项目创建的副本仍通过 `.git/info/exclude` 仅保留在本机。
- 解析出的中央路径改为保存在目标仓库本地 `.git/config`，确保公开启动器始终通用。
- 在整个实现中彻底区分中央代码位置、目标项目位置和私人配置位置。
- 原有个人账号已迁移到 `private/config.txt`，现有 SSH 密钥和项目绑定保持不变。
- 中英文 README 均按照新的集中式安装方式和项目日常流程重新编写。

### 移除

- 移除 `dist/` 目录和完整的个人脚本副本。
- 移除可执行脚本顶部的账号与偏好字段。

## 1.2.0 - 2026-08-31

### 新增

- 第一次在交互式终端中运行时询问一次界面语言，默认使用英文，也可选择完整本地化的中文界面。
- 在菜单中加入界面语言选项，方便以后直接修改已经保存的选择。
- 完成默认流程、更新流程和高级功能的全面双语化，让输入提示、说明、提醒和错误信息始终使用同一种已选语言。

### 变更

- 公开脚本和个人副本的标准文件名由 `go.sh` 统一改为 `g.sh`；复制后仍可按需要自由改名。
- 界面语言和显示模式改为保存在独立脚本顶部清晰、方便人工维护的字段中。
- 不再创建脚本专用的本机偏好设置目录，脚本自行保存所需偏好，继续保持单文件运行。
- 更新流程和高级功能直接沿用已经保存的界面语言，不再分别重复询问。

## 1.1.0 - 2026-08-31

### 新增

- 新增第四个公开命令 `./g.sh update`，用于处理 GitHub 用户名变化、仓库改名或转移，以及两者同时变化的情况。
- 提供默认英文和完整中文本地化的更新引导，只询问当前变化真正需要的信息。
- 写入本机设置前，先核实原有密钥现在对应的新 GitHub 用户名，并确认该账号能够访问准确的新仓库。
- 继续使用原账号密钥，自动创建无冲突的新用户名 SSH 连接，同时保留旧连接，避免影响其他本地项目。
- 自动同步脚本顶部的账号资料、GitHub 隐私邮箱建议、当前项目的提交身份、指定密钥绑定，以及 `origin` 的拉取与推送地址。
- 个人仓库原本由旧用户名持有时自动迁移所属账号；组织仓库保持不变，仓库转移后也可直接粘贴完整 GitHub 地址。

### 修复

- 修复 macOS 对同一个实际文件夹返回不同大小写、路径写法或等价系统前缀时，被误判为“脚本位于另一个 Git 仓库内部”的问题。

## 1.0.0 - 2026-08-31

### 新增

- 提供 `./g.sh`、`./g.sh new` 和 `./g.sh menu` 三个极简入口。
- 自动发现 `~/.ssh/config` 及 `Include` 文件中的现有 GitHub SSH 身份。
- 为每个 GitHub 账号创建独立 ED25519 密钥，并自动处理 `github-USERNAME` 别名冲突。
- 在保存账号或项目绑定前，验证密钥实际登录的 GitHub 用户名。
- 使用 GitHub 公共账号 ID 生成当前格式的 noreply 提交邮箱建议值。
- 将项目本地绑定固定到单个账号、邮箱、SSH 别名和私钥。
- 通过临时 SSH 包装程序强制每次网络 Git 操作只使用指定私钥。
- 支持个人仓库、组织仓库和当前项目账号切换。
- 支持 `owner/repository`、HTTP、HTTPS、SSH、仓库内部网页地址及常见克隆命令输入。
- 实现 `package.json`、主 CHANGELOG、多语言 CHANGELOG、递归 CHANGELOG 和 `VERSION*` 版本发现。
- 支持 CHANGELOG 顶部/底部顺序、多种日期、预发布版本、构建信息和 Unicode 破折号。
- 自动识别深色与浅色环境，并提供高对比度及无颜色模式。
- 支持脚本重命名、顶部账号配置安全自修改和本地自动排除。
- 依据 Git 跟踪状态处理脚本自身，无需配套的源项目标记文件，保持真正的单文件运行。
- 在不增加公开命令的前提下提供双语高级菜单，并默认使用英文界面。
- 支持从完整历史发布版本文件夹按 SemVer 顺序重建线性 Git 历史。
- 使用完整快照替换，保留隐藏文件及被忽略文件，并在所有层级排除 `.git` 和 `.DS_Store`。
- 支持复用可靠的 CHANGELOG 日期、可选轻量 `vX.Y.Z` 版本标签，并为内容相同的版本保留独立提交。
- 历史快照缺少 `.gitignore` 时，允许用户在终端直接提供内容，且只补充缺失版本。
- 上传前检查可能不适合公开的文件，以及 GitHub 无法通过普通推送接收的超大文件。
- 支持向空远端正常推送，并可在明确确认后通过精确的 force-with-lease 替换远端 `main`，不自动创建备份分支。
- 整个用户界面使用纯文字状态标签，不再输出 Emoji 表情符号。
- 提供临时 HOME、临时 Git 仓库、模拟 SSH 传输和严格推送的隔离测试套件。

### 文档

- 提供面向公开仓库访客的 en-US `README.md`。
- 提供信息同步的中文 `README_zh.md`。
- 建立 README 和 CHANGELOG 的双向语言切换链接。
