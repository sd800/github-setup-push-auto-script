<!-- Keep this file synchronized with CHANGELOG.md. -->

# 变更日志

[English](CHANGELOG.md)

Auto Script for GitHub Setup and Push 的重要变更记录在此文件中。

## 3.10.3 - 2026-09-01

### 变更

- 英文 README 与变更日志中的语言链接由 `Chinese` 改为更准确的 `Simplified Chinese`。

## 3.10.2 - 2026-09-01

### 变更

- 将 MIT License 的版权所有者由 `sd800` 改为 `Songming.org`。

## 3.10.1 - 2026-09-01

### 变更

- 日常流程中所有可能耗时的阶段都会先明确说明当前工作，包括检查工作区、查找版本号、暂存、创建提交、建立 SSH 连接和上传。创建提交时还会说明：仓库自己配置的 Git hook 或提交签名可能在此阶段显示额外提示。
- 普通 push 不再等命令结束后一次性显示结果，而是实时转发 Git 提供的 `--progress` 数据。对象统计、压缩、传输和远端返回信息都会在运行过程中出现；同一份输出仍会保留在本次进程中，用于失败后的准确原因说明。
- 历史快照安全检查和历史构建增加 `[当前项/总数]` 进度；普通及带精确租约保护的历史上传都会实时显示 Git 传输过程；日常工作目录衔接本机重建历史前也会明确显示当前阶段。
- 所有进度都必须有真实依据：只能判断阶段时就说明阶段，Git 能提供传输进度时才显示真实数据，不使用无法衡量实际工作的虚假百分比或循环动画。
- 项目正式采用 MIT License，并在仓库根目录加入完整许可文本。

### 测试

- 扩大聚焦 push 检查，验证实时进度输出、明确使用 `origin`、管道中仍能保留 Git 的失败状态，以及原有的详细失败原因分类。

## 3.9.3 - 2026-09-01

### 变更

- 在轻量启动器和中央引擎入口同时加入进程级 `GIT_PAGER=cat` 保护，并保留展示列表时逐条使用的 `--no-pager`。以后新增 Git 命令也会自动采用非交互输出，同时不会修改用户自己的 Git 配置。
- 日常推送使用的指定密钥 SSH 通道和高级身份核对都增加存活检查。连接建立后如果网络中断并持续无响应，会按正常失败返回，不再无限等待；明确显示的密钥口令输入仍可正常使用。

### 测试

- 扩大聚焦检查范围，覆盖轻量启动器、中央引擎的分页器总保护，以及普通和高级 SSH 流程的连接存活设置。

## 3.9.2 - 2026-09-01

### 修复

- 提交改动摘要、状态输出和历史重建日志全部明确禁用 Git 交互式分页器。首次提交包含大量文件时，列表会直接正常输出，不会再停在只有一个 `:` 的画面等待用户按下没有说明的 `q`。

### 测试

- 新增聚焦回归检查：测试环境故意配置一个 Git 分页器，并确认提交流程始终不会启动它。

## 3.9.1 - 2026-09-01

### 变更

- 收紧已完成项目的快速流程：只有仓库所属账号、提交名称、邮箱、SSH 主机名、指定密钥以及 `origin` 的拉取和推送地址全部一致时才会直接使用。中央脚本移动后，只在本地刷新新路径，不会重新设置账号。
- 日常上传始终把当前分支明确推送到 `origin`，其他远端留下的上游设置或默认推送目标不能把本次操作引向别处。
- 项目原来保存的 SSH 主机名缺失或已经改指其他密钥时，只要项目保存的准确原密钥仍在，就为同一把密钥补建不冲突的用户名式主机名；不新建密钥，也不进行联网预检。
- 去掉历史重建在账号选择刚完成后立即重复执行的 SSH 账号核验；新密钥创建后也不再另外调用 `ssh-agent` 预加载一次。

### 修复

- 多密钥主机和已经失效的用户名式主机，只有实际优先选中的密钥正是已验证或已保存的指定密钥时才会被采用。
- 新增 SSH 主机名时保留采用软链接的 SSH 配置文件，安全解析连续文件链接，正确处理密钥路径，并在替换配置目标前核对最终生效的准确密钥。
- 项目保存的自定义 SSH 主机配置缺失时，仍能从该项目自己的准确记录中识别 origin 并完成修复，同时不会放宽为接受任意未配置主机名。
- `update` 会继续使用项目准确保存的原密钥，但不再接受与它不一致的已保存主机名，避免修复主机名时静默换成该主机当前指向的另一把密钥。
- 高级核对重试后发现的身份会统一到稳定的用户名式主机名，不再把旧式多密钥主机直接保存进项目绑定。

### 测试

- 新增聚焦验证，覆盖快速绑定中的提交身份和拉取／推送地址一致性、中央路径移动、准确推送目标、多密钥主机、软链接 SSH 配置、主机名缺失修复、`update` 后备查找、自定义 origin 恢复，以及历史重建只核验一次身份。

## 3.8.1 - 2026-09-01

### 变更

- 在被 Git 忽略的 `private/` 中新增可直接复制的 `g.sh`，其中已经写入当前中央 `git-auto.sh` 的准确位置。运行中央管理程序或中央仓库自己的启动器时会自动刷新这份个人副本，其他项目日常上传时不增加维护操作；根目录由 Git 跟踪的公开 `g.sh` 仍然不包含个人路径。
- 轻量启动器现在既接受完整的 `git-auto.sh` 文件路径，也接受该文件所在文件夹的路径。带引号的路径、`~` 和文件夹路径都会先自动规范化。
- 扩大 SSH 发现范围：除了明确写出的主机配置，也会检查 `github.com` 的实际默认连接、同一 SSH 主机列出的每一把现有密钥，以及尚未分配给任何主机的常见 `~/.ssh/id_*` 私钥；通过 `Include` 和通配规则继承的设置同样包含在内。

### 修复

- 修复新项目首次设置时，未能沿用中央仓库已经保存的非标准 SSH 主机名，因而错误询问是否重复创建密钥的问题。新项目现在会直接复用同一账号在中央仓库中明确绑定的 SSH 主机名和密钥，全程不做联网预检。
- 明确本机身份的采用顺序：当前项目的完整绑定优先，其次是中央仓库保存的完整所属账号—密钥绑定，最后才采用 `github-USERNAME` 命名规则。旧的用户名式主机名不会再抢先覆盖更准确的中央绑定。
- 项目绑定不完整时，只有非标准 SSH 主机名仍然解析到项目所保存的同一把私钥，才会直接信任；不一致时继续进入更安全的身份查找流程。
- 如果这台电脑已有 GitHub 密钥，但仅凭可信本机状态仍无法确定哪一把属于当前账号，普通设置不会猜测，也不会直接进入新建流程；用户可以明确选择高级联网识别、明确选择另建密钥，或者退出并保持 SSH 配置不变。
- 高级联网核对未能确认旧密钥后，后续的新建密钥询问改为默认选择“否”，避免临时网络故障造成重复密钥。
- 高级 SSH 身份识别改用非交互连接；候选密钥已加密、不可用或尚未解锁时会直接返回失败，不会在流程中突然等待用户输入密钥口令。
- 修复 GitHub 用户名更改后同步本机设置时，后备查找错误地使用新用户名，而没有按原用户名寻找该账号原有密钥的问题。

### 测试

- 新增聚焦测试，覆盖中央仓库非标准 SSH 主机名复用、同一主机多密钥发现、以文件夹路径启动，以及个人 `private/g.sh`。

## 3.7.5 - 2026-09-01

### 变更

- 提交说明确认界面改用两行简短操作提示，并把可编辑的默认提交说明单独放在下一行；中英文终端输出都更容易阅读。

## 3.7.3 - 2026-09-01

### 变更

- 脚本创建任何正式提交前，现在都必须先取得用户确认。日常上传和首次提交会在暂存前逐次确认本次提交说明。
- 历史重建会逐项显示准确的 `Release X.Y.Z` 提交说明，并在创建任何提交之前，请用户对整份已列明的提交计划确认一次。

### 修复

- 为历史仓库构建程序增加内部确认状态保护，防止其他调用路径绕过已经展示的版本计划，直接创建提交。

### 测试

- 新增只覆盖提交确认的聚焦测试入口，检查日常确认、暂存前取消、拒绝未确认的历史提交，以及整批确认后允许创建提交；不会连带运行无关测试。

## 3.7.2 - 2026-09-01

### 变更

- 每次创建新提交前都恢复一次提交说明确认，已经完成设置的项目通过 `./g.sh` 快速上传时也不例外。脚本会在 `git add -A` 之前展示根据版本号自动生成的提交说明；直接按 Enter 即可采用，输入其他内容可以替换，输入 `:cancel` 则会在暂存、提交和上传之前停止。
- 已完成设置的项目仍保持极简，只显示提交说明确认；首次设置或本机绑定不完整时，继续额外展示完整的工作区改动。

## 3.7.1 - 2026-09-01

### 变更

- 新增由 Git 跟踪的 `src/option.txt`，以后所有需要长期保存的二元开关只能写在这里。文件只允许使用固定技术标签以及 `enabled`、`disabled` 两种值；当前版本没有这类开关，因此特意保持为空。
- 界面语言、多值的 `display-theme` 显示偏好，以及 GitHub 用户名和邮箱记录继续保存在被忽略的 `private/config.txt` 中。
- 旧版私人配置中如果还留有 `add-tags-to-historical-release` 字段，程序会自动清除，同时完整保留语言、显示模式和账号资料。

### 移除

- 完整移除历史版本 Git 标签功能，包括高级设置、本地建标签、远端标签检查、同名冲突处理和标签上传。历史重建今后只生成按顺序排列的 `Release X.Y.Z` 提交；仓库中原本已经存在的标签不会被删除或改动。
- 移除显示技术详情的功能及其 `GIT_AUTO_DEBUG_DETAILS` 环境开关。目标摘要今后始终只显示 GitHub 账号，并在下一行显示仓库。

### 测试

- 更新配置、历史重建、菜单和目标摘要的回归测试，覆盖空白开关文件以及两项旧功能已经完整移除的行为。

## 3.6.1 - 2026-09-01

### 变更

- 将“快速、方便、简洁；严禁简单问题复杂化”同时确立为产品原则和技术实现规范。普通命令会直接复用完整的本机状态，在必要的 `git push` 之前不再执行任何可选联网预检。
- 普通账号设置、项目绑定、本机项目检查和 `./g.sh update` 不再调用 `curl`、`ssh -T` 或 `git ls-remote`。需要上传时，账号和仓库是否可用统一以实际 `git push` 的返回结果为准。
- “高级功能”新增三项明确的联网工具：通过 SSH 识别并导入现有账号、向 GitHub 核对已保存账号密钥，以及对当前项目执行 SSH 身份验证和仓库只读诊断。
- 普通账号设置只沿用项目已经明确保存的密钥，或者按 `github-USERNAME` 命名的 SSH 主机。没有对应身份时，经确认后为该账号创建独立密钥；高级导入可以验证旧式主机名，并在保留原配置的同时增加稳定的用户名式主机名。
- 提交邮箱建议改为完全在本机生成，不再查询 GitHub API。`ssh-keygen` 也只在当前流程确实需要新建密钥时才作为必要命令。
- `g.sh` 的中央脚本定位缩减为四种准确来源：显式环境变量、当前仓库保存的本机路径、同目录中央脚本，或者用户粘贴的一次性路径。不再扫描上级目录、常见用户目录或 `PATH`。
- 中央入口改为在同一轮中检查并加载各模块。项目账号菜单只绑定仓库所属账号；本机项目检查会核对完整的所属账号、密钥、SSH 主机名以及 origin 拉取和上传地址，不再只看用户名。
- `./g.sh update` 改为专门同步 GitHub 上已经完成更名后的本机设置，全程不联网；仍会沿用原密钥、规范化新用户名的 SSH 主机名，并在写入前完整列出本机改动。
- 历史版本上传完成后，新增日常工作目录衔接流程。默认目标是现有且通常非空的项目目录；脚本保留其中全部文件，只在缺少 Git 记录时执行 `git init`，遇到无关本地历史时先取得确认，再通过 mixed reset 把现有差异保留为未提交改动，最后写入 `g.sh` 和所属账号、密钥、origin 绑定，使后续可以直接正常上传。
- 历史版本标签合并为一次推送；替换远端 `main` 前也不再额外 fetch。精确的 `--force-with-lease` 仍会以原子方式拒绝确认后发生的任何远端变化。

### 修复

- 修复项目检查只因用户名相同就误报“已经就绪”的问题；今后密钥、SSH 主机、拉取地址或上传地址缺失及不一致都会被准确发现。
- 修正文档中已经不符合轻量启动器实现的目录扫描说明。
- 修正批量推送标签时的参数顺序，确保 Git 把 force-with-lease 识别为选项，而不是错误地当作 refspec。

### 测试

- 新增本机账号选择、缺失绑定无联网修复、用户名与仓库改名无预检、标签批量推送，以及联网工具只出现在高级功能中的回归测试。
- 新增历史衔接集成测试，覆盖没有 Git 记录的非空目录和具有无关历史的非空仓库，并验证文件保持不变、mixed reset、启动器安装和后续 `./g.sh` 可用状态。

## 3.5.1 - 2026-08-31

### 变更

- 正式确立“快速、方便、简洁”的产品原则。除非必要信息缺失或已经变化，日常上传不得被扩展成设置流程，严禁把简单问题复杂化。
- 已完成设置的项目运行 `./g.sh` 时，流程精简为与 `git add -A`、有改动时自动执行 `git commit -m "Release X.Y.Z"`、最后 `git push` 实质相同的操作。
- 新增纯本机快速检查，只核对已保存的仓库所属账号、SSH 主机名、指定密钥、拉取目标和上传目标。绑定正确时不再重复执行 `ssh -T`、`git ls-remote`，也不再重复询问提交说明、重写配置、解释状态或展示暂存摘要；整个流程只在 push 时连接一次 GitHub。
- 首次设置以及绑定变化、缺失或不一致时，仍保留完整的引导和验证流程。
- SSH 连接等待改为最长 8 秒且只尝试连接一次，网络不可用时能够尽快返回。
- 上传失败时会保留并说明本地提交仍然存在，显示 Git 的准确返回内容，并分别解释远端历史不一致、账号或仓库被拒绝以及网络连接失败等情况。

### 修复

- 恢复中央仓库根目录中公开、可直接复制的 `g.sh`；迁移后的本地快照曾漏掉这个受 Git 跟踪的启动器。
- 修正快速流程状态与上传退出码的测试方式，确保测试检查的是主 shell 中保留下来的状态和本次上传的准确结果。
- 确认工作区没有新改动时，重新运行 `./g.sh` 仍会继续执行 `git push`；网络中断后可以直接重试，不会重复创建提交。

### 测试

- 新增无前置网络连接的快速流程、SSH 超时、已配置项目自动提交、干净工作区继续上传，以及中英文上传失败说明的回归测试。

## 3.3.1 - 2026-08-31

### 变更

- `./g.sh`、`./g.sh new`、项目核对、改名同步和历史版本重建统一采用同一条账号规则：`owner/repository` 只能使用用户名为 `owner` 的已配置账号。
- 保留的 origin 身份接口及历史重建的内部构建、发布入口也加入 owner 一致性校验；项目中的 `./g.sh new` 未能完成所属账号设置时会直接停止，不再回退到任意账号添加流程。
- 重新撰写远端检查文案，只说明实际完成的只读检查，不再把公开仓库可读误写成已证明仓库归属或上传权限。
- `./g.sh update` 只处理同一账号下的用户名变化和仓库改名；输入其他账号名下的仓库地址时会明确拒绝。
- 中英文 README 已同步说明严格的账号与仓库对应关系，以及远端只读检查的准确边界。

### 修复

- 修复当前仓库保存的错误账号或普通 `github.com` 默认密钥优先于仓库所属账号的问题。此前可能把 `davidsdd` 错误绑定到 `sd800/...`；今后会弃用不一致的旧绑定，并在下次正常运行时自动修正。
- 修复带有明确版本号的首次快照仍建议 `Initial commit` 的问题。只要识别到版本，就始终优先建议 `Release X.Y.Z`。

### 测试

- 新增错误旧绑定与命令账号、保存前 owner 校验、带版本号的首次提交、同账号改名同步以及项目版本号规则的回归测试。

## 3.1.11 - 2026-08-31

### 变更

- 深色模式整套配色再次提高一个亮度层级，并使用更柔和、饱和度更低的 256 色。所有选定颜色在纯黑和常见深灰背景上的对比度均不低于 9.8:1。

## 3.1.10 - 2026-08-31

### 变更

- 高级功能中的设置名称改为“重建历史时添加版本标记”，让选项直接说明会执行的操作。

## 3.1.9 - 2026-08-31

### 变更

- 深色模式的整套配色统一提高亮度，并采用经过柔化的 256 色。所有选定颜色在纯黑和常见深灰背景上的对比度均不低于 7.8:1，同时避免恢复成刺眼的高亮色。

## 3.1.8 - 2026-08-31

### 变更

- 深色模式由基础 ANSI 色改为亮度适中的 256 色，重新调整标题、信息、完成、注意、错误和次要文字。所有选定颜色在纯黑和常见深灰背景上的对比度均高于 5:1。

## 3.1.7 - 2026-08-31

### 变更

- 在高亮绿色及其他状态色仍然偏亮后，移除深色模式剩余的高亮色，标题、完成、注意和错误文字改用基础 ANSI 色。

## 3.1.6 - 2026-08-31

### 变更

- 降低深色模式下信息文字的亮度，由高亮亮青色改为普通青色。

## 3.1.5 - 2026-08-31

### 新增

- 在高级功能中新增历史版本 `vX.Y.Z` 标签设置。标签默认关闭；只有开启时才会在 `private/config.txt` 中写入 `add-tags-to-historical-release: enabled`，关闭后会删除整行设置。
- 新增是否重建提交时间的选择，默认使用可靠识别到的发布日期。选择关闭后不写入历史日期，每个提交保留 Git 创建它时自动记录的本机时间。

### 变更

- 中英文标签设置提示新增完整说明，明确标签会显示在 GitHub 的 Tags 页面，并且不会改变文件内容。

## 3.1.4 - 2026-08-31

### 变更

- 每个需要确认是否可以公开的文件现在都会显示具体原因：文件名通常用于保存凭据、私人配置或密钥材料，或者文件开头符合常见私钥格式。
- 调整风险检查的标题和说明，让用户能够理解并逐项核对，不再只显示含义不明的“可能不适合公开”。

## 3.1.3 - 2026-08-31

### 修复

- 修复历史导入的私钥检查因为扫描器源码中包含自身匹配规则，而把源码误报为私钥文件的问题。
- 私钥内容检查改为识别文件开头完整的密钥格式头。仅提到私钥格式的源码和说明文档不会报警，真实的 OpenSSH、PEM、PGP 和 PuTTY 私钥仍会被识别，同时新增 `.ppk` 文件名识别。

## 3.1.2 - 2026-08-31

### 变更

- 中文“是／否”提示明确显示可输入的快捷字母，统一使用 `[是(y)/否(n)，默认是]` 或 `[是(y)/否(n)，默认否]`。

## 3.1.1 - 2026-08-31

### 新增

- 新增由 7 个文件组成的 `src/` 模块结构，分别承载核心界面与配置、SSH、仓库识别、日常流程、历史导入、改名同步和菜单入口。
- 新增统一分页组件；任何可能超过 8 项的菜单或枚举列表都使用同一规则。每页先使用 `1-8`，再使用允许的字母选项；`x` 查看上一页，`y` 查看下一页，`0` 始终列在最后。
- 新增诊断详情接口 `GIT_AUTO_DEBUG_DETAILS=1`。正常界面不再显示提交作者、提交邮箱、SSH 主机名、私钥路径、上传地址、本地分支和底层推送命令。

### 变更

- `git-auto.sh` 已精简为中央调度入口，会先确认所有必要模块完整存在，再开始加载。它与 `src/` 仍是一套无需第三方依赖的自包含中央安装。
- GitHub 账号在分配菜单选项前，先按照用户名进行不区分大小写的字母排序，确保每次打开菜单时顺序稳定。
- `1-8` 之后的账号选项只使用 `a b c d e f g h j k m n p r`。`s` 和 `w` 留给未来功能；`i`、`l`、`o`、`q`、`t`、`u`、`v` 不作选项使用。
- 已保存账号不超过 8 个时，“添加另一个账号”固定使用 `9`；超过 8 个后固定使用 `z`。
- GitHub 目标摘要默认只按顺序显示账号和仓库。身份及传输细节只在诊断模式中显示，或在解释必要操作和错误时显示。
- 历史版本计划、跳过的文件夹、风险文件和冲突标签列表改用相同分页规则，不再截断长列表，也不会一次输出过多内容。

## 2.1.1 - 2026-08-31

### 变更

- 所有单选菜单统一使用连续数字。选择账号时按 `1`、`2`、`3` 等序号输入；`0` 始终用于返回或取消；针对某项明确操作的确认则继续直接询问“是”或“否”。
- 历史版本导入中的“添加另一个账号”不再使用字母选项，改为紧接现有账号序号的下一个数字。
- 中英文 README 已补充统一的输入规则，并新增回归检查，防止菜单以后重新出现字母选项。

## 2.1.0 - 2026-08-31

### 新增

- 新增明确的项目状态检查阶段：显示准确的仓库根目录和当前分支，保留已有 Git 历史，并在 detached HEAD、冲突未解决或 Git 操作尚未完成时安全停止。
- 新增依据 origin 识别账号的流程：遇到 `git@github800:owner/repository.git` 这类 SSH 地址时，先核对该主机名指定的密钥及其实际 GitHub 用户名，再考虑其他账号。
- 新增暂存前取消、空仓库结束和 update 只读取消保证，并为这些边界补充回归测试。
- 仓库地址新增支持 `git://github.com/owner/repository.git`。

### 变更

- 默认流程重新按照项目状态、origin、账号、具体密钥、目标仓库权限、改动检查、提交和上传的顺序组织。
- 已有仓库会明确说明不会再次执行 `git init`，并保留提交记录、分支、暂存内容和远端设置。
- 中央仓库中由 Git 跟踪的 `g.sh` 现在用于操作中央仓库本身；直接运行 `git-auto.sh` 则始终进入中央管理。
- 多个 SSH 主机名引用同一把私钥时只验证一次，减少重复等待和输出。
- 中英文界面文案全面改为说明每一步涉及的具体密钥、主机名、仓库、账号、文件、命令和写入边界。
- 中英文 README 已同步更新，与实际操作顺序和本机改动范围保持一致。

### 修复

- 修复已有仓库已经使用有效的 GitHub SSH 主机名时，仍进入通用账号创建流程或询问是否另建密钥的问题。
- 修复中文文案中的变量紧接全角标点时，被 Bash 误解析为其他变量名的问题。
- 修复只读识别或取消 update 时仍重写本地中央脚本路径设置的问题。
- 修复中央仓库中由 Git 跟踪的 `g.sh` 被当成其他项目本地启动器处理的问题。

## 2.0.0 - 2026-08-31

### 新增

- 新增唯一的完整中央引擎 `git-auto.sh`，供所有具体项目共同使用。
- 新增由 Git 跟踪的通用根目录 `g.sh`，可直接从公开仓库复制，且不包含个人路径或账号资料。
- 支持自动创建和修复小型项目本地 `g.sh`；启动器继续提供四个日常入口，并把自身文件夹准确传给中央引擎作为项目根目录。
- 新增完整双语的中央管理菜单，统一处理启动器、共享账号、检查修复、偏好设置和高级工具。
- 新增被 Git 忽略的 `private/config.txt`，集中保存界面语言、显示模式和所有共享 GitHub 账号基本资料。
- 自动把私人目录和配置文件权限限制为只有当前用户可以访问。
- 中央文件夹移动后，项目启动器会明确说明原路径失效及修复方法。

### 变更

- 以一个公开中央引擎和一份私人文本配置，取代重复的公开版／个人版完整脚本。
- 所有个性化内容均移出可执行代码；更新账号或偏好时不再修改 `git-auto.sh` 或项目启动器。
- 中央仓库根目录 `g.sh` 保持公开并由 Git 跟踪；中央菜单为其他项目创建的副本仍通过 `.git/info/exclude` 仅保留在本机。
- 解析出的中央路径改为保存在目标仓库本地 `.git/config`，确保公开启动器始终通用。
- 在整个实现中彻底区分中央代码位置、目标项目位置和私人配置位置。
- 原有个人账号已迁移到 `private/config.txt`，现有 SSH 密钥和项目绑定保持不变。
- 中英文 README 均按照新的集中式安装方式和项目日常流程重新编写。

### 移除

- 移除 `dist/` 目录和完整的个人脚本副本。
- 移除可执行脚本顶部的账号与偏好字段。

## 1.2.0 - 2026-08-31

### 新增

- 第一次在交互式终端中运行时询问一次界面语言，默认使用英文，也可选择完整本地化的中文界面。
- 在菜单中加入界面语言选项，方便以后直接修改已经保存的选择。
- 完成默认流程、更新流程和高级功能的全面双语化，让输入提示、说明、提醒和错误信息始终使用同一种已选语言。

### 变更

- 公开脚本和个人副本的标准文件名由 `go.sh` 统一改为 `g.sh`；复制后仍可按需要自由改名。
- 界面语言和显示模式改为保存在独立脚本顶部清晰、方便人工维护的字段中。
- 不再创建脚本专用的本机偏好设置目录，脚本自行保存所需偏好，继续保持单文件运行。
- 更新流程和高级功能直接沿用已经保存的界面语言，不再分别重复询问。

## 1.1.0 - 2026-08-31

### 新增

- 新增第四个公开命令 `./g.sh update`，用于处理 GitHub 用户名变化、仓库改名或转移，以及两者同时变化的情况。
- 提供默认英文和完整中文本地化的更新引导，只询问当前变化真正需要的信息。
- 写入本机设置前，先核实原有密钥现在对应的新 GitHub 用户名，并确认该账号能够访问准确的新仓库。
- 继续使用原账号密钥，自动创建无冲突的新用户名 SSH 连接，同时保留旧连接，避免影响其他本地项目。
- 自动同步脚本顶部的账号资料、GitHub 隐私邮箱建议、当前项目的提交身份、指定密钥绑定，以及 `origin` 的拉取与推送地址。
- 个人仓库原本由旧用户名持有时自动迁移所属账号；组织仓库保持不变，仓库转移后也可直接粘贴完整 GitHub 地址。

### 修复

- 修复 macOS 对同一个实际文件夹返回不同大小写、路径写法或等价系统前缀时，被误判为“脚本位于另一个 Git 仓库内部”的问题。

## 1.0.0 - 2026-08-31

### 新增

- 提供 `./g.sh`、`./g.sh new` 和 `./g.sh menu` 三个极简入口。
- 自动发现 `~/.ssh/config` 及 `Include` 文件中的现有 GitHub SSH 身份。
- 为每个 GitHub 账号创建独立 ED25519 密钥，并自动处理 `github-USERNAME` 别名冲突。
- 在保存账号或项目绑定前，验证密钥实际登录的 GitHub 用户名。
- 使用 GitHub 公共账号 ID 生成当前格式的 noreply 提交邮箱建议值。
- 将项目本地绑定固定到单个账号、邮箱、SSH 别名和私钥。
- 通过临时 SSH 包装程序强制每次网络 Git 操作只使用指定私钥。
- 支持个人仓库、组织仓库和当前项目账号切换。
- 支持 `owner/repository`、HTTP、HTTPS、SSH、仓库内部网页地址及常见克隆命令输入。
- 实现 `package.json`、主 CHANGELOG、多语言 CHANGELOG、递归 CHANGELOG 和 `VERSION*` 版本发现。
- 支持 CHANGELOG 顶部/底部顺序、多种日期、预发布版本、构建信息和 Unicode 破折号。
- 自动识别深色与浅色环境，并提供高对比度及无颜色模式。
- 支持脚本重命名、顶部账号配置安全自修改和本地自动排除。
- 依据 Git 跟踪状态处理脚本自身，无需配套的源项目标记文件，保持真正的单文件运行。
- 在不增加公开命令的前提下提供双语高级菜单，并默认使用英文界面。
- 支持从完整历史发布版本文件夹按 SemVer 顺序重建线性 Git 历史。
- 使用完整快照替换，保留隐藏文件及被忽略文件，并在所有层级排除 `.git` 和 `.DS_Store`。
- 支持复用可靠的 CHANGELOG 日期、可选轻量 `vX.Y.Z` 版本标签，并为内容相同的版本保留独立提交。
- 历史快照缺少 `.gitignore` 时，允许用户在终端直接提供内容，且只补充缺失版本。
- 上传前检查可能不适合公开的文件，以及 GitHub 无法通过普通推送接收的超大文件。
- 支持向空远端正常推送，并可在明确确认后通过精确的 force-with-lease 替换远端 `main`，不自动创建备份分支。
- 整个用户界面使用纯文字状态标签，不再输出 Emoji 表情符号。
- 提供临时 HOME、临时 Git 仓库、模拟 SSH 传输和严格推送的隔离测试套件。

### 文档

- 提供面向公开仓库访客的 en-US `README.md`。
- 提供信息同步的中文 `README_zh.md`。
- 建立 README 和 CHANGELOG 的双向语言切换链接。
